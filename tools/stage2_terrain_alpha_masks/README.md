Stage2 terrain pieces use the alpha channel in
`assets/graphic/stage2_cyber_terrain_sheet.png` by default.

Add per-rect manual mask PNGs here only when a generated piece needs extra
background cleanup. White pixels in a mask are treated as transparent.
