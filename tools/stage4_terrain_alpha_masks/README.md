# Stage4 alpha masks

`tools/run.py stage-alpha-mask-editor --stage 4` opens the manual editor for this atlas.

The committed masks seed transparency from black pixels connected to each crop's border, keeping dark stone detail inside a piece opaque.
